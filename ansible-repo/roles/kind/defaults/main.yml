# This file contains default variables for the "kind" role.
# It can define any default values related to kind installation that can be overridden.

kind_version: "0.11.1"
kind_binary_url: "https://kind.sigs.k8s.io/dl/{{ kind_version }}/kind-linux-amd64"
kind_install_path: "/usr/local/bin/kind"
kind_dependencies:
  - "curl"
  - "docker"