# Install ArgoCD
- name: Add ArgoCD Helm repository
  shell: |
    helm repo add argo https://argoproj.github.io/argo-helm
    helm repo update
  environment:
    KUBECONFIG: /home/k8s/.kube/config

- name: Install or Upgrade ArgoCD
  community.kubernetes.helm:
    name: argocd
    chart_ref: argo/argo-cd
    namespace: argocd
    create_namespace: yes
    state: present
    update_repo_cache: yes
    values:
      server:
        ingress:
          enabled: false
          forceHttps: false
      config:
        rbac:
          policy.default: role:admin
  environment:
    KUBECONFIG: /home/k8s/.kube/config

- name: Wait for ArgoCD server to be ready
  shell: |
    kubectl wait --namespace argocd \
      --for=condition=available deployment \
      --timeout=600s argocd-server
  environment:
    KUBECONFIG: /home/k8s/.kube/config

- name: Create ArgoCD ingress with cert-manager
  shell: |
    kubectl apply -f - <<EOF
    apiVersion: networking.k8s.io/v1
    kind: Ingress
    metadata:
      name: argocd-server
      namespace: argocd
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
    spec:
      ingressClassName: traefik
      tls:
        - secretName: argocd-tls
          hosts:
            - argo.kamos.freeddns.org
      rules:
        - host: argo.kamos.freeddns.org
          http:
            paths:
              - path: /
                pathType: Prefix
                backend:
                  service:
                    name: argocd-server
                    port:
                      number: 80
    EOF
  environment:
    KUBECONFIG: /home/k8s/.kube/config

- name: Grant ArgoCD permissions to deploy in any namespace
  shell: |
    kubectl apply -f - <<EOF
    apiVersion: rbac.authorization.k8s.io/v1
    kind: ClusterRoleBinding
    metadata:
      name: argocd-cluster-admin
    roleRef:
      apiGroup: rbac.authorization.k8s.io
      kind: ClusterRole
      name: cluster-admin
    subjects:
    - kind: ServiceAccount
      name: argocd-application-controller
      namespace: argocd
    EOF
  environment:
    KUBECONFIG: /home/k8s/.kube/config

- name: Configure ArgoCD to deploy applications from argocd directory
  shell: |
    kubectl apply -f - <<EOF
    apiVersion: argoproj.io/v1alpha1
    kind: Application
    metadata:
      name: kamos-cluster-apps
      namespace: argocd
    spec:
      project: default
      source:
        repoURL: 'https://github.com/Yayg/kamos-ansible.git'
        targetRevision: HEAD
        path: argocd
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
    EOF
  environment:
    KUBECONFIG: /home/k8s/.kube/config
